import { typeCreator } from '../shared';

export const {
  ADD_HOODS,
  CLEAR_HOODS_ON_POINT,
  DESELECT_HOOD,
  SELECT_HOOD,
  SET_HOOD_NAME,
  SET_HOODS_ON_POINT,
  SET_HOODS,
  TOGGLE_HOOD_SELECTED,
} = typeCreator;
